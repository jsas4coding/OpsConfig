# v3.0.4

## Added

- **Uninstall vim during cleanup**: When running as root, the installer now removes `vim` and `vim-runtime` packages since OpsConfig uses Neovim as the editor.

## Changed

- Bumped version to 3.0.4
